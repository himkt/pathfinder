{
  "mcpServers": {
    "pathfinder": {
      "command": "pathfinder",
      "args": [
        "-w", ".",
        "-e", "py",
        "-e", "pyi",
        "-s", "uv", "run", "pyright-langserver", "--", "--stdio"
      ],
      "env": {
        "LOG_LEVEL": "debug"
      }
    }
  }
}
